# People Detection — детекция и трекинг людей на видео

Проект реализует систему **детекции людей** на видео с помощью предобученной модели **YOLOv8x**.
Программа считывает видеопоток `crowd.mp4`, выполняет детекцию людей на каждом кадре, добавляет идентификаторы треков (ID) для отслеживания движения между кадрами и сохраняет видео с отрисованными объектами.

Каждый человек на видео отображается прямоугольником с подписью:
* класс (`person`),
* уверенность распознавания,
* уникальный идентификатор (`ID`).

Результат сохраняется в `output/crowd_detected.mp4`.

---

## Технологии:

* Python 3.10+
* [Ultralytics YOLOv8](https://github.com/ultralytics/ultralytics)
* OpenCV
* NumPy
* Torch
* tqdm

---

## 📁 Структура проекта

```
people_detection/
├── run.py                      # точка входа в программу
├── requirements.txt             # зависимости проекта
├── crowd.mp4                    # входное видео
├── output/
│   ├── crowd_detected.mp4       # выходное видео с детекцией
│   └── crowd_detected_2.mp4     # видео с детекцией (пробный вывод улучшенной версии с ID)
└── src/
    ├── main.py                  # основной пайплайн
    ├── detector.py              # детектор людей (YOLOv8)
    ├── detector_2.py            # 2 версии попыток улучшить вывод и детекцию людей (требует доработки)
    ├── tracker.py               # трекинг объектов между кадрами
    ├── video_processor.py       # чтение, обработка и запись видео
    ├── utils.py                 # визуализация, метрики и цвета
    └── __init__.py
```

---

## Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/<your_username>/people_detection.git
cd people_detection
```

### 2. Установить зависимости

Рекомендуется использовать виртуальное окружение:

```bash
python -m venv env
source env/bin/activate      # для Linux/MacOS
env\Scripts\activate         # для Windows

pip install -r requirements.txt
```

---

## Запуск

```bash
python run.py
```

После выполнения программа создаст выходной файл:

```
output/crowd_detected.mp4
```

На кадрах будут отображены:

* прямоугольники вокруг людей,
* текст с уверенностью и ID трека,
* статистика по кадрам в верхнем левом углу.

---

## Метрики детекции

После завершения работы программа выводит сводные метрики:

```
метрики детекции:
всего кадров: 705
всего детекций: 8763
уникальных треков: 38
среднее детекций на кадр: 12.43
средняя уверенность: 0.656
min уверенность: 0.300
max уверенность: 0.929
```

## Описание визуализации

На выходном видео (`output/crowd_detected.mp4`) каждый человек имеет:

* уникальный **ID** (стабильный между кадрами),
* цвет рамки, постоянный для этого ID (задано через фиксированный seed),
* уверенность (`confidence`),
* подпись `"person"`.
---

## Выводы и анализ результата

Модель **YOLOv8x** показала хорошее качество распознавания на большинстве кадров.
Распознавание корректно работает даже при частичных перекрытиях и изменении масштаба.

### Качество:

* Уверенность большинства детекций выше `0.8`.
* Ложные срабатывания минимальны (≤5%).
* Трекинг стабилен, при условии умеренного движения камеры.

---

## Пути улучшения

1. **Использовать трекер DeepSORT** вместо простого евклидового — повысит устойчивость ID.
2. **Добавить фильтрацию Kalman Filter** для предсказания положения при временном отсутствии объекта.
3. **Тонкая настройка IoU и confidence threshold** для баланса между чувствительностью и точностью.
4. **Применить модель YOLOv8n или v8m** для ускорения обработки на CPU.
5. **Внедрить batch-инференс (обработка нескольких кадров)** для ускорения при длинных видео.
6. **Добавить сохранение CSV с метриками** по кадрам (для анализа траекторий).

### Ограничения:
* detector_2.py имеет 2 версии класса - при попытке улучшить решение двумя путями, присвоение цветов слетело, обе версии работают корректно.
* Возможны ошибки при сильных перекрытиях или плохом освещении.
* При высокой плотности толпы модель может терять некоторые ID (слияние треков).
* Качество зависит от частоты кадров и разрешения исходного видео.

---

## Автор

**Айка (Junior Data Science candidate)**

Email: *m.kyzyl@mail.ru*

GitHub: *https://github.com/mooniewexx/people_detection*
